using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This namespace was created to have the same enum list in multiple scripts 
namespace SpellType
{
    public enum Type { None, Normal, Fire, Water, Earth}    
}
